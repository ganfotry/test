pipeline {
    agent any

    stages {
        stage('Dsk') {
            steps {
                sh 'df -h'
            }
        }
        stage('list') {
            steps {
                sh 'ls'
            }
        }
    }
}